// todo: Its output is HTML collection, which like an array
// const element = document.getElementsByClassName('cl')
// for (let i = 0; i < element.length; i++) {
//     console.log(element[i].innerHTML);
// }

//? If we want to change innerHTML. We observe that innerHTML of lists having class "cl" is changed to "<h1>Final List</h1>" 
// const element = document.getElementsByClassName('cl')
// for (let i = 0; i < element.length; i++) {    
//     element[i].innerHTML = "<h1>Final List</h1>" 
// }
